<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- 1. 타임라인 상단 수직 선 -->
    <View
        android:id="@+id/v_timeline_top_line"
        android:layout_width="2dp"
        android:layout_height="0dp"
        android:background="#E2E8F0"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/cv_timeline_dot"
        app:layout_constraintStart_toStartOf="@id/cv_timeline_dot"
        app:layout_constraintEnd_toEndOf="@id/cv_timeline_dot" />

    <!-- 2. 타임라인 하단 수직 선 -->
    <View
        android:id="@+id/v_timeline_bottom_line"
        android:layout_width="2dp"
        android:layout_height="0dp"
        android:background="#E2E8F0"
        app:layout_constraintTop_toTopOf="@id/cv_timeline_dot"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/cv_timeline_dot"
        app:layout_constraintEnd_toEndOf="@id/cv_timeline_dot" />

    <!-- 3. 타임라인 도트 (원형 뷰) -->
    <View
        android:id="@+id/cv_timeline_dot"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:background="@drawable/bg_timeline_dot"
        app:layout_constraintTop_toTopOf="@id/layout_history_info"
        app:layout_constraintBottom_toBottomOf="@id/layout_history_info"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="24dp" />

    <!-- 4. 변경 상세 정보 -->
    <LinearLayout
        android:id="@+id/layout_history_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/cv_timeline_dot"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 상태 칩 -->
        <TextView
            android:id="@+id/tv_history_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_status_chip"
            android:paddingHorizontal="10dp"
            android:paddingVertical="4dp"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="#475569"
            tools:text="신규접수" />

        <!-- 날짜 -->
        <TextView
            android:id="@+id/tv_history_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textSize="13sp"
            android:textColor="#64748B"
            tools:text="2026-03-08 14:00:00" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
