<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginTop="10dp">

    <!-- 읽기 모드 (기본 표시) -->
    <LinearLayout
        android:id="@+id/layoutReadMode"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:orientation="vertical"
        android:background="@drawable/bg_edit_text"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:visibility="visible">

        <TextView
            android:id="@+id/tvCompletedLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/gray_text"
            android:textSize="12sp"
            android:includeFontPadding="false" />

        <!-- 일반 값 표시용 (이름, 업체명, 번호 등) -->
        <TextView
            android:id="@+id/tvCompletedValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:textStyle="bold"
            android:includeFontPadding="false"
            android:visibility="visible" />

        <!-- 이메일 전용 정렬 값 표시용 -->
        <LinearLayout
            android:id="@+id/layoutEmailCompletedValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="8dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/tvCompletedEmailId"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:textSize="18sp"
                android:textStyle="bold"
                android:includeFontPadding="false" />

            <TextView
                android:id="@+id/tvCompletedEmailDomain"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:textColor="@color/gray_text"
                android:textSize="18sp"
                android:includeFontPadding="false" />
        </LinearLayout>
    </LinearLayout>

    <!-- 인라인 편집 모드 (처음 입력창과 동일하게 둥근 박스 내부에 라벨과 입력 필드가 상하 공존) -->
    <LinearLayout
        android:id="@+id/layoutEditMode"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:orientation="vertical"
        android:background="@drawable/bg_edit_text"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/tvEditLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/gray_text"
            android:textSize="12sp"
            android:includeFontPadding="false" />

        <EditText
            android:id="@+id/etEditValue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@android:color/transparent"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:imeOptions="actionDone"
            android:padding="0dp"
            android:includeFontPadding="false"
            android:singleLine="true" />
    </LinearLayout>

    <!-- 편집 에러 메시지는 박스 아래에 노출 -->
    <TextView
        android:id="@+id/tvEditError"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="@color/color_error"
        android:textSize="11sp"
        android:visibility="gone" />

</LinearLayout>
