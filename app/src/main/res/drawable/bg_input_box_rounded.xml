<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 에러 상태 (isActivated = true) -->
    <item android:state_activated="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <corners android:radius="12dp" />
            <stroke android:width="1.2dp" android:color="#EF4444" />
        </shape>
    </item>
    <!-- 포커스 상태 (isSelected = true 또는 state_focused = true) -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <corners android:radius="12dp" />
            <stroke android:width="1.2dp" android:color="#3B82F6" />
        </shape>
    </item>
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <corners android:radius="12dp" />
            <stroke android:width="1.2dp" android:color="#3B82F6" />
        </shape>
    </item>
    <!-- 기본 상태 (밝고 얇은 연회색 테두리) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="#E2E8F0" />
        </shape>
    </item>
</selector>
